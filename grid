<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        grid map
    </title>
    <style>

        .cell {
            width: 50px;
            height: 50px;
            /*border:1px;*/
            display: inline-block;
        }

        .start {
            background-color: green;
        }
        .end {
            background-color: red;
        }
        .obstacle {
            background-color: grey;
        }

    </style>
    
</head>

<body>
    <div>
    <h1 class= "test"align="center"  onmouseenter="handleMouseOver">
        Grid Map
    </h1>

    <div id="grid" align="center">
        {%for row in grid %}
        <div id="row">
            {%for cell in row %}
           <label class="cell" id="cell">{{cell}}</label>
           {% endfor %}
        </div>
        {% endfor %}
    </div>

    <script>
        function handleMouseOver() {
            alert("Mouse hovered over the div!");
        }
        //handleMouseOver();
    </script>

    <script>
        let startcell = null;
        let endcell = null;
        

        var test = document.querySelectorAll("#grid #row .cell");
        var h1 = document.querySelector(".test");
        h1.addEventListener("click",function(){
            alert("hiosefhosef");
        });

        test.forEach(function(label){
                label.addEventListener("mouseover",function(){
                    alert("fjriogfhriogrg");
            });
        });

        /*test.addEventListener("click",function(){
            alert("fjriogfhriogrg");
        });

        /*document.addEventListener("DOMContentLoaded"), function(){
            const text = document.getElementsByClassName(".cell");

            text.addEventListener("mouseover",function(){
                console.log("fheiurfghwieg");
                alert("fhuirghiur");
            });
        };
        /*document.querySelectorall('.cell').foreach(cell => {
            cell.addEventListener('click',() => {
                const[row,col] = cell.id.split('-').map(Number);
                if (cell.classList.contains('start')) {
                    cell.classList.remove('start');
                    startCell = null;
                } else if (cell.classList.contains('end')) {
                    cell.classList.remove('end');
                    endCell = null;
                } else if (cell.classList.contains('obstacle')) {
                    cell.classList.remove('obstacle');
                } else {
                    if (!startCell) {
                        cell.classList.add('start');
                        startCell = [row, col];
                    } else if (!endCell) {
                        cell.classList.add('end');
                        endCell = [row, col];
                    } else {
                        cell.classList.add('obstacle');
                    }
                }
            });
        });*/
    </script>
    
</body>

</html>
